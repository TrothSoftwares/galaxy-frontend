
<div class="ui inverted menu sec3-h" style="padding:0px 0px 0px 0px !important; " >
  {{#link-to 'dashboard.enquiry-forms' }}
  <div class="item">
    <img src="images/bk2.png" alt="" />
  </div>
  {{/link-to}}
  <div class="ui header  item">
  Follow Up for Enquiry #{{enquiry.id}}
</div>
</div>

<div class="ui inverted small form sec3-con">
  <div class="ui inverted dividing header">
    Followup Info
  </div>


          <div class="field">
            <label>Client Message</label>
            {{input placeholder="Client Message" type="text" value=message}}
          </div>

          <div class="field">
            <label>Remarks</label>
            {{input placeholder="Remarks" type="text" value=remarks}}
          </div>
          <div class="two fields">
            <div class="field">
              <label>Date</label>
              {{pikaday-input  placeholder="Select Date" value=date}}
            </div>

          <div class="field">
            <label>Followup Date</label>
            {{pikaday-input  placeholder="Select Date" value=followupdate}}
          </div>

          <div class=" field ">
            <label>Status</label>

      <select onchange={{action "selectStatus" value="target.value"}} class="form-control">
              {{#each statuses as |statusChoice|}}
                <option value={{statusChoice}} selected={{eq enquiry.status statusChoice}}>{{statusChoice}}</option>
              {{/each}}
        </select>
    </div>


        </div>

        <div class="two fields">


        </div>
        <div class="ui blue small submit button {{isCreateFollowupButtonDisabled}}"   {{action "updateFollowup"}}>Save</div>
        {{#if (eq enableNewSale false)}}

        {{#link-to 'dashboard.sales.new' }}
<div class="ui blue small submit button }}"   >Sell</div>
        {{/link-to}}
        {{/if}}
        {{#if (eq enableNewSale true)}}
        <div class="eight wide column">
          <div class="ui dividing inverted header">Sales Info </div>

          <div class="field ">
            <label>Total Price</label>
            {{input placeholder="Total Price"  type="text"   value=totalprice}}
          </div>
          <div class="field">
            <label>Installments / Month</label>
            {{input placeholder="Installments / Month"  type="text" value=installpricepermonth}}
          </div>

              <div class="ui blue small submit button {{iscreateSaleButtonEnabled}}"   {{action "sellToEnquiry"}}>Confirm</div>

        </div>
        {{/if}}


      </div>






      <div class="ui inverted small form sec3-con">
        <div class="ui inverted dividing header">
          Basic Info
        </div>


                <div class="field">
                  <label>Name</label>
                  {{input placeholder="Name"  type="text"   value=enquiry.name}}
                </div>

                <div class="field">
                  <label>Address</label>
                  {{input placeholder="Address"  type="text" value=enquiry.address}}
                </div>
                <div class="two fields">
                  <div class="field">
                    <label>Contact</label>
                    {{input placeholder="Contact" type="text" value=enquiry.contact}}
                  </div>

                <div class="field">
                  <label>Email</label>
                  {{input placeholder="Email" type="text" value=enquiry.email}}
                </div>



              </div>

              <div class="two fields">


              </div>
              <div class="ui blue small submit button {{isupdateEnquiryButtonDisabled}}"   {{action "updateEnquiry"}}>Save</div>


            </div>
