

<div class="ui inverted menu sec3-h" style="padding:0px 0px 0px 0px !important; " >
  {{link-to 'New Customer' 'dashboard.customers.new'  class="ui blue inverted mini right floated button "}}
  <!-- {{#link-to 'dashboard.enquiry-forms' }}
  <div class="item">
    <img src="images/bk2.png" alt="" />
  </div>
  {{/link-to}} -->
  <div class="ui header item">
  Customers
</div>
</div>
<div class="sec3-con" >
  <table class="ui celled structured table">
  <thead>
    <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Contact</th>
    <th>Email</th>
    <th>View</th>
   </tr></thead>
  <tbody>
    {{#each customers as |customer index|}}
    <tr>
      <td>{{incrementone index}}</td>
      <td>{{customer.name}}</td>
      <td>{{customer.contact}}</td>
      <td>{{customer.email}}</td>
      <td>{{link-to "View"  'dashboard.customers.customer.view'  customer }}</td>
    </tr>
    {{/each}}
  </tbody>
</table>
</div>

{{outlet}}
