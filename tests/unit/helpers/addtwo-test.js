import { addtwo } from 'galaxy/helpers/addtwo';
import { module, test } from 'qunit';

module('Unit | Helper | addtwo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = addtwo([42]);
  assert.ok(result);
});
